# Berachain Contract ABIs

This will create abis for all contracts on Berachain to use as a reference.

## Quick Start

```bash
# FROM: ./

chmod +x ./abi.sh;
```

Start:

```bash
# FROM: /

./abi.sh;
```